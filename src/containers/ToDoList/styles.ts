import styled from 'styled-components';

export const Container = styled.main`
	padding: 0 40px;
	height: 100vh;
	overflow-y: scroll;
`;

export const Result = styled.p`
	display: block;
	font-weight: bold;
	font-size: 18px;
	margin: 40px auto;
`;
